{
  "name": "@mosaic/web",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit -p tsconfig.build.json && vite build",
    "preview": "vite preview",
    "test": "vitest run",
    "test:watch": "vitest",
    "test:coverage": "vitest run --coverage",
    "test:e2e": "playwright test"
  },
  "dependencies": {
    "@mosaic/wasm":  "workspace:*",
    "@mosaic/logic": "workspace:*"
  },
  "devDependencies": {
    "@playwright/test": "1.59.1",
    "fast-check": "^4.8.0",
    "jsdom": "^29.1.1",
    "typescript": "^6.0.3",
    "vite": "^8.0.13",
    "vite-plugin-wasm": "^3.6.0",
    "vitest": "^4.1.6"
  }
}
